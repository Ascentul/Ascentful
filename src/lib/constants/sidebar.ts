/**
 * Sidebar dimension constants
 * Used for consistent positioning of sidebar-related elements
 */

// Sidebar widths in pixels
export const SIDEBAR_WIDTH_EXPANDED = 258;
export const SIDEBAR_WIDTH_COLLAPSED = 80;

// Position for expand button when sidebar is collapsed
// Positioned inside the white content card, past the rounded corner (sidebar 80px + gap + card padding)
export const SIDEBAR_EXPAND_BUTTON_LEFT = 116;
