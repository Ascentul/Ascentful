# Ascentul Autofill Extension Environment Variables

# The base URL of the Ascentul web application
PLASMO_PUBLIC_APP_URL=http://localhost:3000

# Extension OAuth state signing secret (must match server-side EXTENSION_STATE_SECRET)
# Generate with: openssl rand -base64 32
PLASMO_PUBLIC_STATE_SECRET=your-state-secret-here
